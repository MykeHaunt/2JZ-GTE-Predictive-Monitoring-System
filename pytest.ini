[[pytest]
minversion = 7.0
testpaths = 
    backend/tests
python_files = test_*.py
addopts = --cov=backend --cov-report=term --cov-report=xml --strict-markers
markers =
    unit: mark a test as a unit test
    integration: mark a test as an integration test
    slow: mark test as slow (excluded by default)